# Kernel SVM Variants — Benchmark Classification
Systematic benchmark of kernel SVM variants on standard classification datasets.
### ■ Research Highlights
- **Kernel Zoo**: Evaluates RBF, polynomial, sigmoid, and a custom $\chi^2$ kernel for histogram features ac- **Calibration Analysis**: Applies Platt scaling and isotonic regression to SVM outputs, reporting reliabil- **Algorithms Focus**: Profiles training time vs. accuracy for each kernel, showing RBF dominates on most ta**Tech Stack:** Python, `scikit-learn`, `NumPy`.
